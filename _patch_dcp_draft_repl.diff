Binary files k3_sharded/v1/__pycache__/kv_cache_interface.cpython-312.pyc and k3_repl/v1/__pycache__/kv_cache_interface.cpython-312.pyc differ
Binary files k3_sharded/v1/core/__pycache__/kv_cache_coordinator.cpython-312.pyc and k3_repl/v1/core/__pycache__/kv_cache_coordinator.cpython-312.pyc differ
Binary files k3_sharded/v1/core/__pycache__/kv_cache_utils.cpython-312.pyc and k3_repl/v1/core/__pycache__/kv_cache_utils.cpython-312.pyc differ
Binary files k3_sharded/v1/core/__pycache__/single_type_kv_cache_manager.cpython-312.pyc and k3_repl/v1/core/__pycache__/single_type_kv_cache_manager.cpython-312.pyc differ
diff --git a/v1/core/kv_cache_coordinator.py b/v1/core/kv_cache_coordinator.py
--- a/v1/core/kv_cache_coordinator.py
+++ b/v1/core/kv_cache_coordinator.py
@@ -846,6 +846,9 @@
                     dcp_world_size=(
                         self.dcp_world_size
                         if isinstance(spec, FullAttentionSpec)
+                        and not getattr(
+                            spec, "non_causal_multi_token_decode", False
+                        )  # K3-DCP-DRAFT-REPL
                         else 1
                     ),
                 )
diff --git a/v1/core/kv_cache_utils.py b/v1/core/kv_cache_utils.py
--- a/v1/core/kv_cache_utils.py
+++ b/v1/core/kv_cache_utils.py
@@ -651,8 +651,13 @@
 def resolve_dcp_kv_block_size(spec: KVCacheSpec, dcp_world_size: int) -> int:
     """Return the token span of a cache block under DCP."""
     layer_specs = iter_layer_specs(spec)
-    if len(layer_specs) > 0 and all(
-        isinstance(layer_spec, AttentionSpec) for layer_spec in layer_specs
+    if (
+        len(layer_specs) > 0
+        and all(isinstance(layer_spec, AttentionSpec) for layer_spec in layer_specs)
+        # K3-DCP-DRAFT-REPL: replicated draft group keeps its unscaled per-rank span.
+        and not any(
+            getattr(ls, "non_causal_multi_token_decode", False) for ls in layer_specs
+        )
     ):
         return spec.block_size * dcp_world_size
     return spec.block_size
diff --git a/v1/core/single_type_kv_cache_manager.py b/v1/core/single_type_kv_cache_manager.py
--- a/v1/core/single_type_kv_cache_manager.py
+++ b/v1/core/single_type_kv_cache_manager.py
@@ -71,6 +71,8 @@
                 block until the request finishes.
         """
         self.scheduler_block_size = scheduler_block_size
+        if getattr(kv_cache_spec, "non_causal_multi_token_decode", False):
+            dcp_world_size = 1  # K3-DCP-DRAFT-REPL: replicated draft group
         # The block size for this manager; used for actual block allocation.
         self.block_size = kv_cache_spec.block_size
         self.dcp_world_size = dcp_world_size
diff --git a/v1/kv_cache_interface.py b/v1/kv_cache_interface.py
--- a/v1/kv_cache_interface.py
+++ b/v1/kv_cache_interface.py
@@ -428,6 +428,11 @@
     def max_num_blocks_per_req(self, vllm_config: VllmConfig, max_len: int) -> int:
         parallel_config = vllm_config.parallel_config
         kv_shard_count = parallel_config.decode_context_parallel_size
+        if getattr(self, "non_causal_multi_token_decode", False):
+            # K3-DCP-DRAFT-REPL: the replicated non-causal DSpark draft runs its attention at
+            # dcp=1, so its KV is not sharded and its block-table row must be
+            # full width. Sizing it at cdiv(L, block*dcp) is an 8x row overrun.
+            kv_shard_count = 1
         return cdiv(max_len, self.block_size * kv_shard_count)
 
 
Binary files k3_sharded/v1/worker/gpu/__pycache__/block_table.cpython-312.pyc and k3_repl/v1/worker/gpu/__pycache__/block_table.cpython-312.pyc differ
Binary files k3_sharded/v1/worker/gpu/__pycache__/model_runner.cpython-312.pyc and k3_repl/v1/worker/gpu/__pycache__/model_runner.cpython-312.pyc differ
diff --git a/v1/worker/gpu/block_table.py b/v1/worker/gpu/block_table.py
--- a/v1/worker/gpu/block_table.py
+++ b/v1/worker/gpu/block_table.py
@@ -26,7 +26,11 @@
         cp_size: int = 1,
         cp_rank: int = 0,
         cp_interleave: int = 1,
+        cp_sizes: list[int] | None = None,
     ):
+        # K3-DCP-DRAFT-REPL: per-group CP degree. Uniform == cp_size except for the
+        # replicated non-causal draft group, which is 1.
+        self.cp_sizes = list(cp_sizes) if cp_sizes else [cp_size] * len(block_sizes)
         self.block_sizes = block_sizes
         self.kernel_block_sizes = kernel_block_sizes
         self.max_num_reqs = max_num_reqs
@@ -100,6 +104,9 @@
         self.kernel_block_sizes_tensor = torch.tensor(
             self.kernel_block_sizes, dtype=torch.int32, device=self.device
         )
+        self.cp_sizes_tensor = torch.tensor(
+            self.cp_sizes, dtype=torch.int32, device=self.device
+        )
         self.input_block_table_ptrs = self._make_ptr_tensor(self.input_block_tables)
 
     def append_block_ids(
@@ -202,6 +209,7 @@
             self.block_table_strides,
             self.block_sizes_tensor,
             self.kernel_block_sizes_tensor,
+            self.cp_sizes_tensor,
             slot_mappings,
             slot_mappings.stride(0),
             self.cp_rank,
@@ -274,6 +282,7 @@
     block_table_strides,  # [num_kv_cache_groups]
     block_sizes,  # [num_kv_cache_groups]
     kernel_block_sizes,  # [num_kv_cache_groups]
+    cp_sizes,  # [num_kv_cache_groups]
     slot_mappings_ptr,  # [num_kv_cache_groups, max_num_tokens]
     slot_mappings_stride,
     cp_rank,
@@ -302,6 +311,9 @@
     block_table_stride = tl.load(block_table_strides + group_id)
     kv_block_size = tl.load(block_sizes + group_id)
     kernel_block_size = tl.load(kernel_block_sizes + group_id)
+    # K3-DCP-DRAFT-REPL: per-group CP degree; 1 for the replicated draft group.
+    cp_size = tl.load(cp_sizes + group_id)
+    eff_cp_rank = tl.where(cp_size == 1, 0, cp_rank)
 
     req_state_idx = tl.load(idx_mapping + batch_idx)
     start_idx = tl.load(query_start_loc + batch_idx)
@@ -316,11 +328,13 @@
             is_local = True
         else:
             # Context parallelism is used.
-            virtual_block_size = kv_block_size * CP_SIZE
+            virtual_block_size = kv_block_size * cp_size
             virtual_block_indices = positions // virtual_block_size
             virtual_block_offsets = positions % virtual_block_size
-            is_local = virtual_block_offsets // CP_INTERLEAVE % CP_SIZE == cp_rank
-            rounds = virtual_block_offsets // (CP_INTERLEAVE * CP_SIZE)
+            is_local = (
+                virtual_block_offsets // CP_INTERLEAVE % cp_size == eff_cp_rank
+            )
+            rounds = virtual_block_offsets // (CP_INTERLEAVE * cp_size)
             remainder = virtual_block_offsets % CP_INTERLEAVE
             local_offsets = rounds * CP_INTERLEAVE + remainder
             local_positions = virtual_block_indices * kv_block_size + local_offsets
diff --git a/v1/worker/gpu/model_runner.py b/v1/worker/gpu/model_runner.py
--- a/v1/worker/gpu/model_runner.py
+++ b/v1/worker/gpu/model_runner.py
@@ -598,6 +598,13 @@
             additional_attn_cg_support=additional_attn_cg_support,
         )
 
+        # K3-DCP-DRAFT-REPL: replicated draft groups stay at cp=1.
+        cp_sizes_per_group = [
+            1
+            if getattr(g.kv_cache_spec, "non_causal_multi_token_decode", False)
+            else self.dcp_size
+            for g in kv_cache_config.kv_cache_groups
+        ]
         self.block_tables = BlockTables(
             block_sizes=block_sizes,
             max_num_reqs=self.max_num_reqs,
@@ -608,6 +615,7 @@
             cp_size=self.dcp_size,
             cp_rank=self.dcp_rank,
             cp_interleave=self.cp_interleave,
+            cp_sizes=cp_sizes_per_group,  # K3-DCP-DRAFT-REPL
         )
         self.pcp_manager = pcp.maybe_build_pcp_manager(
             self.vllm_config,
Binary files k3_sharded/v1/worker/gpu/spec_decode/dflash/__pycache__/speculator.cpython-312.pyc and k3_repl/v1/worker/gpu/spec_decode/dflash/__pycache__/speculator.cpython-312.pyc differ
diff --git a/v1/worker/gpu/spec_decode/dflash/speculator.py b/v1/worker/gpu/spec_decode/dflash/speculator.py
--- a/v1/worker/gpu/spec_decode/dflash/speculator.py
+++ b/v1/worker/gpu/spec_decode/dflash/speculator.py
@@ -407,7 +407,10 @@
                 self.block_tables.input_block_tables[gid],
                 self.block_tables.kernel_block_sizes[gid],
                 self.block_tables.cp_rank,
-                self.block_tables.cp_size,
+                # K3-DCP-DRAFT-REPL: draft KV group is replicated (cp=1)
+                # while the target is DCP-sharded; use this group's CP
+                # degree, not the global one.
+                self.block_tables.cp_sizes[gid],
                 self.block_tables.cp_interleave,
                 self.parallel_drafting_token_id,
                 self.num_query_per_req,
diff --git a/v1/worker/gpu/attn_utils.py b/v1/worker/gpu/attn_utils.py
--- a/v1/worker/gpu/attn_utils.py
+++ b/v1/worker/gpu/attn_utils.py
@@ -292,6 +292,18 @@
         group_is_prefilling = common_attn_metadata_extra_kwargs.pop(
             "is_prefilling", is_prefilling
         )
+        # K3-DCP-DRAFT-REPL: the replicated draft group runs its attention at
+        # cp=1, so its entire context is local to this rank. dcp_local_seq_lens
+        # is computed once from the GLOBAL dcp_size, so handing it to this group
+        # would claim only seq_len/dcp_size is resident and the draft would
+        # attend over 1/dcp_size of its own KV (reading zeros for the rest ->
+        # acceptance length collapses to ~1.0). Use the unscaled seq_lens.
+        group_spec = kv_cache_config.kv_cache_groups[i].kv_cache_spec
+        group_dcp_local_seq_lens = (
+            seq_lens
+            if getattr(group_spec, "non_causal_multi_token_decode", False)
+            else dcp_local_seq_lens
+        )
         common_attn_metadata = CommonAttentionMetadata(
             query_start_loc=query_start_loc_gpu,
             query_start_loc_cpu=query_start_loc_cpu,
@@ -304,7 +316,7 @@
             block_table_tensor=block_table,
             slot_mapping=slot_mapping,
             causal=group_causal,
-            dcp_local_seq_lens=dcp_local_seq_lens,
+            dcp_local_seq_lens=group_dcp_local_seq_lens,
             positions=positions,
             is_prefilling=group_is_prefilling,
             mm_req_doc_ranges=mm_req_doc_ranges,
diff --git a/vllm/model_executor/layers/attention/mla_attention.py b/vllm/model_executor/layers/attention/mla_attention.py
--- a/vllm/model_executor/layers/attention/mla_attention.py
+++ b/vllm/model_executor/layers/attention/mla_attention.py
@@ -548,6 +548,19 @@
             indexer=indexer,
             **extra_impl_args,
         )
+        # K3-DCP-DRAFT-REPL: same reason as the metadata builder -- this layer's KV
+        # group is replicated, so the impl must not do round-robin CP indexing or a
+        # cross-rank LSE combine. The impl reads decode_context_parallel_size in its
+        # own __init__, so override it here, after construction, together with the
+        # values derived from it. rocm_aiter_mla reads dcp_world_size LIVE for
+        # dcp_heads, so only the cached ones need resetting.
+        if self.non_causal_multi_token_decode:
+            self.impl.dcp_world_size = 1
+            for _attr in ("dcp_rank", "_dcp_rank"):
+                if hasattr(self.impl, _attr):
+                    setattr(self.impl, _attr, 0)
+            if hasattr(self.impl, "_decode_num_heads"):
+                self.impl._decode_num_heads = self.num_heads
         self.q_pad_num_heads = getattr(self.impl, "q_pad_num_heads", None)
         self.is_amx_bmm_enabled = getattr(self.impl, "uses_amx_bmm", False)
         # AMX reads kv_b_proj's weight directly and never calls it live; the
@@ -2275,6 +2288,13 @@
         except AssertionError:
             # DCP might not be initialized in testing
             self.dcp_world_size = 1
+        # K3-DCP-DRAFT-REPL: the replicated non-causal DSpark draft group keeps a
+        # full copy of its KV on every rank, so its attention must run at cp=1.
+        # self.non_causal_multi_token_decode came from THIS group's kv_cache_spec
+        # above, so every DCP-derived quantity below (workspace split, prefill
+        # chunk alignment, decode seq_lens / max_seq_len) follows it.
+        if self.non_causal_multi_token_decode:
+            self.dcp_world_size = 1
         self.dcp_local_block_size = parallel_config.cp_kv_cache_interleave_size
         self.dcp_virtual_block_size = self.dcp_local_block_size * self.dcp_world_size
         self.cp_kv_cache_interleave_size = parallel_config.cp_kv_cache_interleave_size
